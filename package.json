{
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.9.0",
    "concurrently": "^9.0.1"
  },
  "scripts": {
    "start": "concurrently \"npm run start:client\" \"npm run start:server\"",
    "start:client": "npm --prefix web-frontend run serve",
    "start:server": "npm --prefix server-backend run dev"
  },
  "dependencies": {
    "body-parser": "^1.20.3"
  }
}
